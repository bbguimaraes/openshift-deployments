Create objects.

    oc create -f deploy.yaml

Replace volumes.

    oc set volume deploymentconfig/php \
        --name pkg --overwrite \
        --type persistentVolumeClaim --claim-name nextcloud-pkg
    oc set volume deploymentconfig/nginx \
        --name pkg --overwrite \
        --type persistentVolumeClaim --claim-name nextcloud-pkg

Build.

    oc start-build --wait memcached &
    oc start-build --wait php &
    oc start-build --wait nginx &
    wait

Deploy.

    oc rollout latest deploymentconfig/memcached
    oc rollout latest deploymentconfig/php
    oc rollout latest deploymentconfig/nginx

Create route.

    oc create route edge \
        --hostname nextcloud.example.com \
        --service nginx
