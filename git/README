Create objects.

    oc create -f deploy.yaml

Replace volumes.

    oc set volume deploymentconfig/git-server-uwsgi \
        --name git --overwrite \
        --type persistentVolumeClaim --claim-name git
    oc set volume deploymentconfig/git-server-nginx \
        --name git --overwrite \
        --type persistentVolumeClaim --claim-name git

Build.

    oc start-build --wait uwsgi &
    oc start-build --wait nginx &
    wait

Deploy.

    oc rollout latest deploymentconfig/git-server-uwsgi
    oc rollout latest deploymentconfig/git-server-nginx

Create route.

    oc create route edge \
        --hostname git.example.com \
        --service nginx
