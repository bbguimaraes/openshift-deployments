Create objects.

    oc create -f deploy.yaml

Replace volume.

    oc set volume deploymentconfig/postgresql \
        --name srv --overwrite \
        --type persistentVolumeClaim --claim-name static

Build.

    oc start-build --wait static

Deploy.

    oc rollout latest deploymentconfig/static

Create route.

    oc create route edge \
        --hostname example.com \
        --service static
